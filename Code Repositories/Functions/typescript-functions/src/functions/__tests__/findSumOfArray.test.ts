import { describe, it, expect } from 'vitest';
// Uncomment the import below when you export your function
// import findSumOfArray from '../findSumOfArray.js';

describe('findSumOfArray', () => {
  it.skip('should return sum of array', () => {
    // const result = findSumOfArray([1, 2, 3, 4, 5]);
    // expect(result).toBe(15);
  });

  it.skip('should return 0 for empty array', () => {
    // const result = findSumOfArray([]);
    // expect(result).toBe(0);
  });

  it.skip('should handle negative numbers', () => {
    // const result = findSumOfArray([-1, -2, -3]);
    // expect(result).toBe(-6);
  });
});
